"""Programme ayant pour objectif d'assurer que mes favs soundcloud sont téléchargés sur mon PC et sur mon portable"""

import soundcloud

import csv
import requests

client = soundcloud.Clien(client_id="KJMNWGMczDAEDm2ezWCbt5kCYZzcr92P")  # todo : à compléter

def save_favorites():

    """Create CSV file with headers and then saves the users like to the CSV file."""

    with open("soundcloud_favs.csv", 'w', encoding='UTF-8') as csv_file:
    
        # csv_file.write("Track Title, Track URL\n")  # writes headers to CSV file
        favs_list = client.get("/me/favorites")
        csv_writer = cs.writer(csv_file)
        csv_writer.writerow(['Track Title', 'Track URL'])
        for track in favs_list:

            csv_writer.writerow([track.title, track.permalink_url])
    



save_favorites()



